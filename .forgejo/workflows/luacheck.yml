name: luacheck

on:
    push:
        branches:
            - master
    pull_request:

jobs:
    luacheck:
        runs-on: codeberg-tiny
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            - name: Luacheck
              uses: lunarmodules/luacheck@master
